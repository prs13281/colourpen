<!--each文をテンプレート化-->
<% comments.each do |comment| %>
  <%= comment.comment %>
  <!--サインインしているユーザーのみに編集、削除ボタン表示-->
  <%# if user_signed_in? %>
    <%# if current_user.id == c.user_id %>
      <%#= link_to "削除", post_comment_path(@post, c), method: :delete %>
    <%# elsif current_user.id == @post.user_id then %>
      <%#= link_to "削除", post_comment_path(@post, c), method: :delete %>
    <%# end %>
  <%# end %>
  <%= link_to "削除する", post_comment_path(@post.id, comment.id), method: :delete, remote: true, class:'btn btn-danger' %>
<% end %>
